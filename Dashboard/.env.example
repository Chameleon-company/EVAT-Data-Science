# API network settings
EVAT_API_HOST=127.0.0.1
EVAT_API_PORT=8000

# Streamlit network settings
EVAT_DASHBOARD_PORT=8501

# Optional: full base URL override used by dashboard requests
# EVAT_API_BASE_URL=http://127.0.0.1:8000

# Price prediction API settings
PRICE_API_HOST=127.0.0.1
PRICE_API_PORT=8001

# Optional: full base URL override used by price dashboard requests
# PRICE_API_BASE_URL=http://127.0.0.1:8001

# Optional: data/model path overrides (relative paths are resolved from Dashboard/)
# EVAT_STATIONS_DATA_PATH=data/EVAT.chargers.csv
# EVAT_CONGESTION_MODEL_PATH=models/congestion_prediction/random_forest_model.pkl
# EVAT_CONGESTION_TRAIN_DATA_PATH=data/congestion_prediction/train_exogenous_3h.csv

# Optional: price model/data path overrides (relative paths are resolved from Dashboard/)
# PRICE_MODEL_PATH=../Use_Cases/Price Prediction/artifacts/price_best_model_latest.joblib
# PRICE_DATA_PATH=../Use_Cases/Price Prediction/artifacts/car_price_enriched_latest.csv
# PRICE_ALT_DATA_PATH=../Use_Cases/Price Prediction/car_price_prediction_enriched_features.csv
# PRICE_FEATURE_DICT_PATH=../Use_Cases/Price Prediction/artifacts/feature_dictionary.csv
